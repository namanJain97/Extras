# Launching the Blotter
    driver.execute_script(
    "await FSBL.Clients.LauncherClient.spawn(arguments[0], arguments[1]);",
    "Blotter | TNTR Reconciliation Break Manager", {"addToWorkspace": True}
    )
    print("Launching Blotter")
    time.sleep(5)
