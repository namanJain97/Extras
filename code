import scipy.stats as stats
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt
import pandas as pd
import scipy.stats as stats


print('checkpoint')


data1 = pd.DataFrame({
    'TradeID': [1, 2, 3, 4, 5, 6] ,  
     'Prodid': [2, 4, 6, 8, 10,3]   
})


df1 = pd.read_csv(r'rawdata.csv')
df1=df1.head(50)

categorical_cols = df1.select_dtypes(include=['object']).columns
df1[categorical_cols] = df1[categorical_cols].apply(lambda x: pd.Categorical(x).codes)

df2 = pd.read_csv(r'sampled_data.csv')
print('checkpoint2')

#pairplot start
print("pairplot starrt")
sns.set()
# plotting the scatter plt for each feature at once
sns.pairplot(df2[categorical_cols])
print("pairplot end")

